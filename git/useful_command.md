사용한 command 를 정리해 보았습니다.

git remote -v : 리모트 저장소의 주소까지 확인합니다.
git remote add [name] [url] : 리모트 저장소를 추가합니다.

git pull : 리모트 저장소의 내용을 현재 저장소에 덮어씌웁니다(fatch+merge)

git add -f : .gitignore 파일에 있는 제외 리스트를 포함해서 add 합니다.

